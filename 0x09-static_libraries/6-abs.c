#include "main.h"

/**
 * _abs - function that prints value of an integer
 * @n: number to be checked
 *
 * Return: always 0 (success)
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
